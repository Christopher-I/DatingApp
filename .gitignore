__pycache__/
*.pyc
.env
*.db
*.sqlite3
.venv/
venv/
.pytest_cache/
.DS_Store
# Never commit anything that could be a raw photo — the design keeps them in
# memory only, but this is a belt-and-suspenders guard.
*.jpg
*.jpeg
*.png
*.webp
